# 托福英语项目 · CLAUDE.md

Fish 的 **沉浸式英语学习系统** 大仓库（前身「托福英语」，文件夹名暂不改）。顶层门户 `index.html` 分两轴：**🎯 应试轴**＝托福全套子系统（单词记忆系统 voca / 听力复述·面试问答 托福口语 / 阅读训练 / 模考 / 写作 / 模考复习 / 背词计划）；**🌊 沉浸轴**＝把英语活成日常，refresh my mind（`日常表达/` 英语日记·批改loop · `文化阅读/` 世界之窗双语精读 · `英语思维/` 思维卡）。各系统统一 `data/*.json → build.py → 自包含 index.html`，全离线、双击即用；新模块也走同一门户与背词汇入规则。

---

## 🔴 核心原则：单词一律汇入背词系统（全项目唯一背词入口）

**本项目任何子系统只要新增或改动了材料（加词 / 加文段 / 加题 / 加范文…），其涉及的单词都必须同步进入背词系统 `背词计划/`，并体现在 `背词计划/index.html`。**

- **背词系统（`背词计划/`）是全项目单词的唯一汇聚点**——Fish 只在这一个页面背词，就能背到项目里遇到的所有词，绝不遗漏散落在各子系统里。
- **触发时机**：给模考复习系统 / 写作系统 / 阅读训练系统 / voca 等**加词、加文段、加范文、加真题**时，**同一批涉及的单词要一并写入背词系统**——这是每次改动的收尾步骤，不做完不算做完。
- **落地机制**：
  - 词写入背词系统的**汇聚词库** `背词计划/data/项目生词.json`（✅ 已建：`build.py` 里 `trim_proj` 已注册为第 3 个词库「项目生词」，词库切换是数据驱动，append 词即自动出现）。直接往该文件的 `words[]` 追加词对象。
  - **汇入的词必须满配**（对齐 voca sheep 标准）：`word/pronunciation/definition` + 富化字段 `ety(词源)·ph(发音规律)·tip(记忆钩子)·xex(例句≥2,[{en,zh,src}])·syn(近义[{w,ipa,note}])·nu(辨析总结)`，`ant` 可选。字段名就是这些缩写（背词卡片按它们渲染）。词多可 fan out 子代理并行富化。
  - 带**来源标注** `src_from`（如「模考4.0·学术阅读」）便于回溯。
  - 词写好后跑 `python3 背词计划/build.py` 重建 `index.html`，改动才算落地到页面。
  - 已在绿皮书/BEAT 词库里的词：可只做掌握标记/富化；库里没有的新词：进汇聚词库。
- **本质**：这是 cc 的工作规则——每次在本项目改动后主动执行，不是文件监听 hook。

---

## 各子系统一句话（改动时对应加词入背词系统）

| 子系统 | 产什么词 |
|---|---|
| 模考复习系统 | 每场模考阅读/听力的生词 + 长难句词 |
| 写作系统 | 范文/例句里的高级词、搭配 |
| 阅读训练系统 | 文章生词 |
| voca 单词记忆系统 | 每组新词（本就是词，天然汇入）|
| 日常表达（沉浸轴）| 日记/对话批改里抽的地道表达 + 生词（src_from「日常表达·日记」）|
| 文化阅读（沉浸轴）| 每篇精读的生词 + 值得记的表达（src_from「文化阅读·世界之窗」）|
| 英语思维（沉浸轴）| 思维卡例句里的实词 |
